<h2>Contact List</h2>

<a href="/addContact" button type= "button" class="btn btn-secondary">Tambah Data</a>

<form action="/contact/checkbox" method="post">

<% if (cont.length > 0) { %>
<div class="table-responsive">
    <table class="table table-bordered table-striped table-hover table-light">
        <thead>
            <tr>
                <th>No.</th>
                <th>Nama</th>
                <th>Nomor HP</th>
                <th>Aksi</th>
                <th>Checkbox</th>
            </tr>
        </thead>
        <tbody>
            <tr>
            <% cont.forEach((cont, i) => { %>
                <td><%= i + 1 %></td>
                <td><%= cont.name %></td>
                <td><%= cont.mobile %></td>
                <td>
                    <a href="/detail/<%= cont.name %>" button type= button class="btn btn-primary">Detail</a>
                </td>
                <td>
                    <input type="checkbox" name="checkboxDelete" value="<%= cont.name %>">
                </td>
            </tr>
            <% }); %>
        </tbody>
    </table>
</div>
<button type="submit" class="btn btn-danger">Delete</button>
</form>
<% } else { %>
    <p>No contact uploaded</p>
<% } %> 